void Sleep() {
  
  // Diplay Animation
// Sleep1
display.clearDisplay();
display.drawBitmap(0, 0, Sleep1, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep2
display.clearDisplay();
display.drawBitmap(0, 0, Sleep2, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep3
display.clearDisplay();
display.drawBitmap(0, 0, Sleep3, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep4
display.clearDisplay();
display.drawBitmap(0, 0, Sleep4, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep5
display.clearDisplay();
display.drawBitmap(0, 0, Sleep5, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep6
display.clearDisplay();
display.drawBitmap(0, 0, Sleep6, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep7
display.clearDisplay();
display.drawBitmap(0, 0, Sleep7, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep8
display.clearDisplay();
display.drawBitmap(0, 0, Sleep8, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep9
display.clearDisplay();
display.drawBitmap(0, 0, Sleep9, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep10
display.clearDisplay();
display.drawBitmap(0, 0, Sleep10, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep11
display.clearDisplay();
display.drawBitmap(0, 0, Sleep11, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep12
display.clearDisplay();
display.drawBitmap(0, 0, Sleep12, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep13
display.clearDisplay();
display.drawBitmap(0, 0, Sleep13, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep14
display.clearDisplay();
display.drawBitmap(0, 0, Sleep14, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep15
display.clearDisplay();
display.drawBitmap(0, 0, Sleep15, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep16
display.clearDisplay();
display.drawBitmap(0, 0, Sleep16, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep17
display.clearDisplay();
display.drawBitmap(0, 0, Sleep17, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep18
display.clearDisplay();
display.drawBitmap(0, 0, Sleep18, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep19
display.clearDisplay();
display.drawBitmap(0, 0, Sleep19, 128, 64, 1);
display.display();
delay(frame_delay);

// Sleep20
display.clearDisplay();
display.drawBitmap(0, 0, Sleep20, 128, 64, 1);
display.display();
delay(frame_delay);


if (frame_delay>50) frame_delay=frame_delay-20;
loop();  
}